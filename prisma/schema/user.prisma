model User {
  id                   Int                   @id @default(autoincrement())
  name                 String
  email                String                @unique
  password             String
  role                 UserRole
  userReviews          UserReview[]
  recentlyViewedMovies RecentlyViewedMovie[]
}

enum UserRole {
  USER
  ADMIN
}
 